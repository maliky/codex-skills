interface:
  display_name: "Long Memory Retrieval"
  short_description: "Recover and prune local Codex memory"
  default_prompt: "Use $long-memory-retrieval to recover prior sessions, decisions, recurring workflows, and attention-suppression candidates from this local Codex repository."

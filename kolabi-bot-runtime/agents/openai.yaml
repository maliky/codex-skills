interface:
  display_name: "Kolabi Bot Runtime"
  short_description: "Maintain typed kolabi runtime transitions"
  default_prompt: "Use $kolabi-bot-runtime to maintain kolabi State + Event -> Commands behavior, tail lifecycle transitions, and runtime boundaries."

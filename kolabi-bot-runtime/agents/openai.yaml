interface:
  display_name: "Kolabi Bot Runtime"
  short_description: "Maintain typed runtime transitions and tail-command boundaries for kolabi."
  default_prompt: "Use $kolabi-bot-runtime to maintain State + Event -> State + Commands behavior, tail lifecycle transitions, and runtime safety boundaries in kolabi."

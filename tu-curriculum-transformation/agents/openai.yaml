interface:
  display_name: "TU Curriculum Transformation"
  short_description: "Normalize TU curricula into Org"
  default_prompt: "Use $tu-curriculum-transformation to normalize TU curriculum sources into Org with course macros, tables, and export checks."

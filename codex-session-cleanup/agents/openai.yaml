interface:
  display_name: "Codex Session Cleanup"
  short_description: "Remove exact local Codex sessions safely"
  default_prompt: "Use $codex-session-cleanup to remove local Codex sessions by exact id or alias with schema-aware verification."

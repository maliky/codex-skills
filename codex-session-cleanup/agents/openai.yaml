interface:
  display_name: "Codex Session Cleanup"
  short_description: "Remove local Codex session state"
  default_prompt: "Use $codex-session-cleanup to remove one or more local Codex sessions by exact id or exact alias and verify the cleanup."
